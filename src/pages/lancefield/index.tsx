import * as React from 'react';
import App from '../../components/App/App';
import { Typography } from '@mui/material';

const AboutLancefield = () => {
	return (
		<App>
			<Typography variant='h1' >AboutLancefield</Typography>
		</App>
	);
};

export default AboutLancefield;
